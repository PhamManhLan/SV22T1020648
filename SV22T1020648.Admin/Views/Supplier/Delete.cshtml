@model SV22T1020648.Models.Partner.Supplier
@{
    ViewBag.Title = "Xóa nhà cung cấp";
    bool canDelete = ViewBag.CanDelete ?? true;
}

@section PageToolBar {
    <div class="d-flex">
        @if (canDelete)
        {
            <button type="submit" form="formDelete" class="btn btn-danger">
                <i class="bi bi-trash me-1"></i> Xóa
            </button>
        }
        <a href="~/Supplier" class="btn btn-secondary ms-1">
            <i class="bi bi-arrow-left"></i> Quay lại
        </a>
    </div>
}

<div class="row justify-content-center">
    <div class="col-12">
        <div class="card @(canDelete ? "card-danger" : "card-warning") card-outline">
            <div class="card-header">
                <h5 class="card-title mb-0 @(canDelete ? "text-danger" : "text-warning")">
                    <i class="bi bi-exclamation-triangle me-2"></i>
                    @(canDelete ? "Xác nhận xóa nhà cung cấp" : "Thông báo không thể xóa")
                </h5>
            </div>

            <div class="card-body">
                @if (canDelete)
                {
                    <div class="alert alert-warning d-flex align-items-center">
                        <i class="bi bi-info-circle me-2"></i>
                        Bạn có chắc chắn muốn xóa nhà cung cấp này không? Hành động này không thể hoàn tác.
                    </div>
                }
                else
                {
                    <div class="alert alert-danger d-flex align-items-center">
                        <i class="bi bi-slash-circle me-2"></i>
                        Không thể xóa nhà cung cấp này vì đang có dữ liệu liên quan.
                    </div>
                }

                <form id="formDelete" method="post" action="~/Supplier/Delete/@Model.SupplierID">
                    <dl class="row mb-0">
                        <dt class="col-sm-4">Tên nhà cung cấp</dt>
                        <dd class="col-sm-8">@Model.SupplierName</dd>

                        <dt class="col-sm-4">Tên giao dịch</dt>
                        <dd class="col-sm-8">@Model.ContactName</dd>

                        <dt class="col-sm-4">Điện thoại</dt>
                        <dd class="col-sm-8">@Model.Phone</dd>

                        <dt class="col-sm-4">Email</dt>
                        <dd class="col-sm-8">@Model.Email</dd>

                        <dt class="col-sm-4">Địa chỉ</dt>
                        <dd class="col-sm-8">@Model.Address</dd>

                        <dt class="col-sm-4">Tỉnh/Thành</dt>
                        <dd class="col-sm-8">@Model.Province</dd>
                    </dl>
                </form>
            </div>
        </div>
    </div>
</div>